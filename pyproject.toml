[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "openedx-site"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["openedx-platform"]

[tool.uv.sources]
openedx-platform = { path = "../openedx-platform" }
